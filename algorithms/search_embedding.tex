\documentclass[varwidth]{standalone}

\usepackage{amsmath}
\usepackage{xcolor}
\usepackage[utf8]{inputenc}
\usepackage[ruled,vlined]{algorithm2e}

\newcommand\mycommfont[1]{\footnotesize\ttfamily\textcolor{blue}{#1}}
\SetCommentSty{mycommfont}

\begin{document}

\begin{minipage}{.9\textwidth}
    \begin{algorithm}[H]
        \DontPrintSemicolon

        \KwIn{$\Lambda = \lbrace \lambda_1, \dots, \lambda_m \rbrace$, $U = \lbrace \mathbf{u}_1, \dots, \mathbf{u}_m \rbrace$}
        \KwOut{$\mathbf{x}_q^*$, $Q$}
        % \KwData{Testing set $x$}

        $n_{\text{sub-DS}}$ = ($\lambda_k$ == 0)  \tcp*{Compute the number of attractors}
        $U_{clusters} = \lbrace \mathbf{u}_{\lambda=0}^1,\dots,\mathbf{u}_{\lambda=0}^{n_{\text{sub-DS}}} \rbrace$ \tcp*{Right "stationary" eigenvectors}
        \While{stop = false}
        {
        stop = false \;
        \tcc{From the first non-stationary eigenvector till the last one}
        \For{$i=n_{\text{attractors}} + 1$ {\bfseries to} $m$}
        {
        \tcc{Iterate over the number of attractors}
        \For{$k=1$ {\bfseries to} $n_{\text{attractors}}$}
        {
            \tcc{Assign eigenvector to sub-dynamics/attractor $k$}
            \If{$\mathbf{1}^T(u_{\lambda=0}^k \circ |u_i|) \neq 0$}
            {$U^k \leftarrow u_i$}
        }
        \tcc{Stop loop at the spectral gap}
        \If{$|\lambda_{i+1} - \lambda_i| > tol$}
        {stop = true}
        }
        }

        \caption{Clustering Dynamics \& Embedding Reconstruction.}
        \label{alg:clustering}
    \end{algorithm}
\end{minipage}

\end{document}